#!/usr/bin/env node
"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.writeConfig = exports.readConfig = exports.openConnection = void 0;
// TODO put node banks and vaults inside the GroupConfig
const fs = __importStar(require("fs"));
const os = __importStar(require("os"));
const yargs_1 = __importDefault(require("yargs/yargs"));
const helpers_1 = require("yargs/helpers");
const web3_js_1 = require("@solana/web3.js");
const commands_1 = require("./commands");
const config_1 = require("./config");
const client_1 = require("./client");
const utils_1 = require("./utils");
const layout_1 = require("./layout");
const anchor_1 = require("@project-serum/anchor");
const entropy_logs_json_1 = __importDefault(require("./entropy_logs.json"));
const _1 = require(".");
const changeMaxAccounts_1 = __importDefault(require("./commands/changeMaxAccounts"));
const bn_js_1 = require("bn.js");
const clusterDesc = [
    'cluster',
    {
        describe: 'the cluster to connect to',
        default: 'mainnet',
        choices: ['devnet', 'mainnet'],
    },
];
const configDesc = [
    'config',
    {
        describe: 'the config file to store all public keys',
        default: __dirname + '/ids.json',
        type: 'string',
    },
];
const keypairDesc = [
    'keypair',
    {
        describe: 'the keypair used to sign all transactions',
        default: os.homedir() + '/.config/solana/entropy-mainnet-authority.json',
        type: 'string',
    },
];
const groupDesc = [
    'group',
    { describe: 'the entropy group name 🥭', type: 'string' },
];
const symbolDesc = [
    'symbol',
    { describe: 'the base token symbol', type: 'string' },
];
function openConnection(config, cluster) {
    return new web3_js_1.Connection(config.cluster_urls[cluster], 'confirmed');
}
exports.openConnection = openConnection;
function readKeypair(keypairPath) {
    return new web3_js_1.Account(JSON.parse(fs.readFileSync(keypairPath, 'utf-8')));
}
function readConfig(configPath) {
    return new config_1.Config(JSON.parse(fs.readFileSync(configPath, 'utf-8')));
}
exports.readConfig = readConfig;
function writeConfig(configPath, config) {
    fs.writeFileSync(configPath, JSON.stringify(config.toJson(), null, 2));
}
exports.writeConfig = writeConfig;
(0, yargs_1.default)((0, helpers_1.hideBin)(process.argv)).command('init-group <group> <entropyProgramId> <serumProgramId> <quote_mint> <fees_vault>', 'initialize a new group', (y) => {
    return y
        .positional(...groupDesc)
        .positional('entropyProgramId', {
        describe: 'the program id of the entropy smart contract',
        type: 'string',
    })
        .positional('serumProgramId', {
        describe: 'the program id of the serum dex smart contract',
        type: 'string',
    })
        .positional('quote_mint', {
        describe: 'the mint of the quote currency 💵',
        type: 'string',
    })
        .positional('fees_vault', {
        describe: 'the quote currency vault owned by Entropy DAO token governance',
        type: 'string',
    })
        .option('quote_optimal_util', {
        describe: 'optimal utilization interest rate param for quote currency',
        default: 0.7,
        type: 'number',
    })
        .option('quote_optimal_rate', {
        describe: 'optimal interest rate param for quote currency',
        default: 0.06,
        type: 'number',
    })
        .option('quote_max_rate', {
        describe: 'max interest rate param for quote currency',
        default: 1.5,
        type: 'number',
    })
        .option('valid_interval', {
        describe: 'the interval where caches are no longer valid',
        default: 15,
        type: 'number',
    })
        .option('symbol', {
        describe: 'the quote symbol',
        default: 'USDC',
        type: 'string',
    })
        .option(...clusterDesc)
        .option(...configDesc)
        .option(...keypairDesc);
}, (args) => __awaiter(void 0, void 0, void 0, function* () {
    console.log('init_group', args);
    const entropyProgramId = new web3_js_1.PublicKey(args.entropyProgramId);
    const serumProgramId = new web3_js_1.PublicKey(args.serumProgramId);
    const quoteMint = new web3_js_1.PublicKey(args.quote_mint);
    const feesVault = new web3_js_1.PublicKey(args.fees_vault);
    const account = readKeypair(args.keypair);
    const config = readConfig(args.config);
    const cluster = args.cluster;
    const connection = openConnection(config, cluster);
    const result = yield (0, commands_1.initGroup)(connection, account, cluster, args.group, entropyProgramId, serumProgramId, args.symbol, quoteMint, feesVault, args.valid_interval, args.quote_optimal_util, args.quote_optimal_rate, args.quote_max_rate);
    console.log(result);
    config.storeGroup(result);
    writeConfig(args.config, config);
    process.exit(0);
})).argv;
(0, yargs_1.default)((0, helpers_1.hideBin)(process.argv)).command('add-oracle <group> <symbol>', 'add an oracle to the group', (y) => {
    return y
        .positional(...groupDesc)
        .positional(...symbolDesc)
        .option('provider', {
        describe: 'oracle provider',
        default: 'stub',
        choices: ['stub', 'pyth', 'switchboard'],
    })
        .option(...clusterDesc)
        .option(...configDesc)
        .option(...keypairDesc);
}, (args) => __awaiter(void 0, void 0, void 0, function* () {
    console.log('add_oracle', args);
    const account = readKeypair(args.keypair);
    const config = readConfig(args.config);
    const cluster = args.cluster;
    const connection = openConnection(config, cluster);
    const group = config.getGroup(cluster, args.group);
    let result;
    if (args.provider === 'pyth') {
        console.log('adding pyth oracle');
        result = yield (0, commands_1.addPythOracle)(connection, account, group, args.symbol);
    }
    else if (args.provider === 'switchboard') {
        console.log('adding switchboard oracle');
        result = yield (0, commands_1.addSwitchboardOracle)(connection, account, group, args.symbol);
    }
    else if (args.provider === 'stub') {
        console.log('adding stub oracle');
        result = yield (0, commands_1.addStubOracle)(connection, account, group, args.symbol);
    }
    else {
        throw new Error();
    }
    config.storeGroup(result);
    writeConfig(args.config, config);
    process.exit(0);
})).argv;
(0, yargs_1.default)((0, helpers_1.hideBin)(process.argv)).command('set-oracle <group> <symbol> <value>', 'set stub oracle to given value', (y) => {
    return y
        .positional(...groupDesc)
        .positional(...symbolDesc)
        .positional('value', {
        describe: 'new oracle value is base_price * quote_unit / base_unit',
        type: 'number',
    })
        .option(...clusterDesc)
        .option(...configDesc)
        .option(...keypairDesc);
}, (args) => __awaiter(void 0, void 0, void 0, function* () {
    console.log('set_oracle', args);
    const account = readKeypair(args.keypair);
    const config = readConfig(args.config);
    const cluster = args.cluster;
    const connection = openConnection(config, cluster);
    const group = config.getGroup(cluster, args.group);
    yield (0, commands_1.setStubOracle)(connection, account, group, args.symbol, args.value);
    process.exit(0);
})).argv;
(0, yargs_1.default)((0, helpers_1.hideBin)(process.argv)).command('change-max-accounts <group> <symbol> <value>', 'set stub oracle to given value', (y) => {
    return y
        .positional(...groupDesc)
        .positional('num_accounts', {
        describe: 'new # max accounts',
        type: 'number',
    })
        .option(...clusterDesc)
        .option(...configDesc)
        .option(...keypairDesc);
}, (args) => __awaiter(void 0, void 0, void 0, function* () {
    console.log('change_max_accounts', args);
    const account = readKeypair(args.keypair);
    const config = readConfig(args.config);
    const cluster = args.cluster;
    const connection = openConnection(config, cluster);
    const group = config.getGroup(cluster, args.group);
    yield (0, changeMaxAccounts_1.default)(connection, account, group, new bn_js_1.BN(args.value));
    process.exit(0);
})).argv;
(0, yargs_1.default)((0, helpers_1.hideBin)(process.argv)).command('add-perp-market <group> <symbol>', 'add a perp market to the group', (y) => {
    return y
        .positional(...groupDesc)
        .positional(...symbolDesc)
        .option('maint_leverage', {
        default: 20,
        type: 'number',
    })
        .option('init_leverage', {
        default: 10,
        type: 'number',
    })
        .option('liquidation_fee', {
        default: 0.025,
        type: 'number',
    })
        .option('maker_fee', {
        default: 0.0,
        type: 'number',
    })
        .option('taker_fee', {
        default: 0.0005,
        type: 'number',
    })
        .option('base_lot_size', {
        default: 100,
        type: 'number',
    })
        .option('quote_lot_size', {
        default: 10,
        type: 'number',
    })
        .option('max_num_events', {
        default: 256,
        type: 'number',
    })
        .option('rate', {
        default: 1,
        type: 'number',
    })
        .option('max_depth_bps', {
        default: 200,
        type: 'number',
    })
        .option('target_period_length', {
        default: 3600,
        type: 'number',
    })
        .option('mngo_per_period', {
        // default: 11400, // roughly corresponds to 100m MNGO per year
        default: 0,
        type: 'number',
    })
        .option('exp', {
        default: 2,
        type: 'number',
    })
        .option(...clusterDesc)
        .option(...configDesc)
        .option(...keypairDesc);
}, (args) => __awaiter(void 0, void 0, void 0, function* () {
    console.log('add-perp-market', args);
    const account = readKeypair(args.keypair);
    const config = readConfig(args.config);
    const cluster = args.cluster;
    console.log('cluster: ', cluster);
    const connection = openConnection(config, cluster);
    const group = config.getGroup(cluster, args.group);
    const result = yield (0, commands_1.addPerpMarket)(connection, account, group, args.symbol, args.maint_leverage, args.init_leverage, args.liquidation_fee, args.maker_fee, args.taker_fee, args.base_lot_size, args.quote_lot_size, args.max_num_events, args.rate, args.max_depth_bps, args.target_period_length, args.mngo_per_period, args.exp);
    config.storeGroup(result);
    writeConfig(args.config, config);
    process.exit(0);
})).argv;
(0, yargs_1.default)((0, helpers_1.hideBin)(process.argv)).command('add-spot-market <group> <symbol> <mint_pk>', 'add a spot market to the group', (y) => {
    return y
        .positional(...groupDesc)
        .positional(...symbolDesc)
        .positional('mint_pk', {
        describe: 'the public key of the base token mint',
        type: 'string',
    })
        .option('market_pk', {
        default: '',
        describe: 'the public key of the spot market',
        type: 'string',
    })
        .option('base_lot_size', {
        default: 100,
        describe: 'Lot size of the base mint',
        type: 'number',
    })
        .option('quote_lot_size', {
        default: 10,
        describe: 'Lot size of the quote mint',
        type: 'number',
    })
        .option('maint_leverage', {
        default: 10,
        type: 'number',
    })
        .option('init_leverage', {
        default: 5,
        type: 'number',
    })
        .option('liquidation_fee', {
        default: 0.05,
        type: 'number',
    })
        .option('optimal_util', {
        describe: 'optimal utilization interest rate param',
        default: 0.7,
        type: 'number',
    })
        .option('optimal_rate', {
        describe: 'optimal interest rate param',
        default: 0.06,
        type: 'number',
    })
        .option('max_rate', {
        describe: 'max interest rate param',
        default: 1.5,
        type: 'number',
    })
        .option(...clusterDesc)
        .option(...configDesc)
        .option(...keypairDesc);
}, (args) => __awaiter(void 0, void 0, void 0, function* () {
    var _a;
    console.log('add-spot-market', args);
    const account = readKeypair(args.keypair);
    const config = readConfig(args.config);
    const cluster = args.cluster;
    console.log('cluster: ', cluster);
    // console.log()
    const connection = openConnection(config, cluster);
    const group = config.getGroup(cluster, args.group);
    const quoteMintPk = (_a = (0, config_1.getTokenBySymbol)(group, group.quoteSymbol)) === null || _a === void 0 ? void 0 : _a.mintKey;
    const market_pk = args.market_pk
        ? new web3_js_1.PublicKey(args.market_pk)
        : yield (0, commands_1.listMarket)(connection, account, group.entropyProgramId, new web3_js_1.PublicKey(args.mint_pk), quoteMintPk, args.base_lot_size, args.quote_lot_size, group.serumProgramId);
    const result = yield (0, commands_1.addSpotMarket)(connection, account, group, args.symbol, market_pk, new web3_js_1.PublicKey(args.mint_pk), args.maint_leverage, args.init_leverage, args.liquidation_fee, args.optimal_util, args.optimal_rate, args.max_rate);
    config.storeGroup(result);
    writeConfig(args.config, config);
    process.exit(0);
})).argv;
(0, yargs_1.default)((0, helpers_1.hideBin)(process.argv)).command('sanity-check <group>', 'check group conditions that always have to be true', (y) => {
    return y.positional(...groupDesc).option(...configDesc);
}, (args) => __awaiter(void 0, void 0, void 0, function* () {
    console.log('sanity check', args);
    const config = readConfig(args.config);
    const groupConfig = config.getGroupWithName(args.group);
    const connection = openConnection(config, groupConfig.cluster);
    yield (0, commands_1.sanityCheck)(connection, groupConfig);
    process.exit(0);
})).argv;
(0, yargs_1.default)((0, helpers_1.hideBin)(process.argv)).command('show <group> <entropy_account_pk>', 'Print relevant details about a entropy account', (y) => {
    return y
        .positional(...groupDesc)
        .positional('entropy_account_pk', {
        describe: 'the public key of the EntropyAccount',
        type: 'string',
    })
        .option(...configDesc);
}, (args) => __awaiter(void 0, void 0, void 0, function* () {
    console.log('show', args);
    const config = readConfig(args.config);
    const groupConfig = config.getGroupWithName(args.group);
    const connection = openConnection(config, groupConfig.cluster);
    const client = new client_1.EntropyClient(connection, groupConfig.entropyProgramId);
    const entropyGroup = yield client.getEntropyGroup(groupConfig.publicKey);
    const entropyAccount = yield client.getEntropyAccount(new web3_js_1.PublicKey(args.entropy_account_pk), groupConfig.serumProgramId);
    const cache = yield entropyGroup.loadCache(connection);
    console.log(entropyAccount.toPrettyString(groupConfig, entropyGroup, cache));
    process.exit(0);
})).argv;
(0, yargs_1.default)((0, helpers_1.hideBin)(process.argv)).command('inspect-wallet <group> <wallet_pk>', 'Print relevant details about a entropy account', (y) => {
    return y
        .positional(...groupDesc)
        .positional('entropy_account_pk', {
        describe: 'the public key of the EntropyAccount',
        type: 'string',
    })
        .option(...configDesc);
}, (args) => __awaiter(void 0, void 0, void 0, function* () {
    console.log('inspect-wallet', args);
    const config = readConfig(args.config);
    const groupConfig = config.getGroupWithName(args.group);
    const connection = openConnection(config, groupConfig.cluster);
    const client = new client_1.EntropyClient(connection, groupConfig.entropyProgramId);
    const entropyGroup = yield client.getEntropyGroup(groupConfig.publicKey);
    const entropyAccounts = yield client.getEntropyAccountsForOwner(entropyGroup, new web3_js_1.PublicKey(args.wallet_pk), false);
    const cache = yield entropyGroup.loadCache(connection);
    for (const entropyAccount of entropyAccounts) {
        console.log(entropyAccount.toPrettyString(groupConfig, entropyGroup, cache));
    }
    process.exit(0);
})).argv;
(0, yargs_1.default)((0, helpers_1.hideBin)(process.argv)).command('decode-log <log_b64>', 'Decode and print out log', (y) => {
    return y
        .positional('log_b64', {
        describe: 'base 64 encoded entropy log',
        type: 'string',
    })
        .option(...configDesc);
}, (args) => __awaiter(void 0, void 0, void 0, function* () {
    console.log('decode-log', args);
    // @ts-ignore
    const coder = new anchor_1.Coder(entropy_logs_json_1.default);
    const event = coder.events.decode(args.log_b64);
    if (!event) {
        throw new Error('Invalid entropy log');
    }
    const data = event.data;
    if (event.name === 'CancelAllPerpOrdersLog') {
        data.allOrderIds = data.allOrderIds.map((oid) => oid.toString());
        data.canceledOrderIds = data.canceledOrderIds.map((oid) => oid.toString());
        data.entropyGroup = data['entropyGroup'].toString();
        data.entropyAccount = data['entropyAccount'].toString();
    }
    else {
        for (const key in data) {
            data[key] = data[key].toString();
        }
    }
    console.log(event);
    process.exit(0);
})).argv;
(0, yargs_1.default)((0, helpers_1.hideBin)(process.argv)).command('show-group <group>', 'Print relevant details about a EntropyGroup', (y) => {
    return y.positional(...groupDesc).option(...configDesc);
}, (args) => __awaiter(void 0, void 0, void 0, function* () {
    console.log('show-group', args);
    const config = readConfig(args.config);
    const groupConfig = config.getGroupWithName(args.group);
    const connection = openConnection(config, groupConfig.cluster);
    const client = new client_1.EntropyClient(connection, groupConfig.entropyProgramId);
    const entropyGroup = yield client.getEntropyGroup(groupConfig.publicKey);
    for (let i = 0; i < layout_1.QUOTE_INDEX; i++) {
        const perpMarket = entropyGroup.perpMarkets[i];
        if (perpMarket.isEmpty()) {
            continue;
        }
        const pmc = (0, config_1.getPerpMarketByIndex)(groupConfig, i);
        const pm = yield client.getPerpMarket(perpMarket.perpMarket, pmc.baseDecimals, pmc.quoteDecimals);
        const x = yield connection.getTokenAccountBalance(pm.mngoVault);
        console.log(pmc.baseSymbol, pm.mngoVault.toBase58(), x);
    }
    process.exit(0);
})).argv;
(0, yargs_1.default)((0, helpers_1.hideBin)(process.argv)).command('show-insurance-vault <group>', 'Print relevant details about a EntropyGroup', (y) => {
    return y.positional(...groupDesc).option(...configDesc);
}, (args) => __awaiter(void 0, void 0, void 0, function* () {
    console.log('show-group', args);
    const config = readConfig(args.config);
    const groupConfig = config.getGroupWithName(args.group);
    const connection = openConnection(config, groupConfig.cluster);
    const vaultBalance = yield connection.getTokenAccountBalance(new web3_js_1.PublicKey('59BEyxwrFpt3x4sZ7TcXC3bHx3seGfqGkATcDx6siLWy'));
    console.log(`Insurance Vault: ${vaultBalance.value.uiAmountString}`);
    process.exit(0);
})).argv;
(0, yargs_1.default)((0, helpers_1.hideBin)(process.argv)).command('show-top-positions <group> <symbol>', 'Print top 10 positions for the symbol perp market', (y) => {
    return y.positional(...groupDesc).option(...configDesc);
}, (args) => __awaiter(void 0, void 0, void 0, function* () {
    console.log('show-top-positions', args);
    const config = readConfig(args.config);
    const groupConfig = config.getGroupWithName(args.group);
    const perpMarketConfig = (0, utils_1.throwUndefined)((0, config_1.getPerpMarketByBaseSymbol)(groupConfig, args.symbol));
    const connection = openConnection(config, groupConfig.cluster);
    const client = new client_1.EntropyClient(connection, groupConfig.entropyProgramId);
    const entropyGroup = yield client.getEntropyGroup(groupConfig.publicKey);
    const entropyAccounts = yield client.getAllEntropyAccounts(entropyGroup, [], false);
    entropyAccounts.sort((a, b) => b.perpAccounts[perpMarketConfig.marketIndex].basePosition
        .abs()
        .cmp(a.perpAccounts[perpMarketConfig.marketIndex].basePosition.abs()));
    const entropyCache = yield entropyGroup.loadCache(connection);
    for (let i = 0; i < 10; i++) {
        console.log(`${i}: ${entropyAccounts[i].toPrettyString(groupConfig, entropyGroup, entropyCache)}\n`);
    }
    process.exit(0);
})).argv;
(0, yargs_1.default)((0, helpers_1.hideBin)(process.argv)).command('show-top-spot-positions <group> <symbol> <deposits_or_borrows>', 'Print top 10 positions for the symbol perp market', (y) => {
    return y.positional(...groupDesc).option(...configDesc);
}, (args) => __awaiter(void 0, void 0, void 0, function* () {
    console.log('show-top-positions', args);
    const config = readConfig(args.config);
    const groupConfig = config.getGroupWithName(args.group);
    const marketIndex = (0, utils_1.throwUndefined)((0, _1.getMarketIndexBySymbol)(groupConfig, args.symbol));
    const connection = openConnection(config, groupConfig.cluster);
    const client = new client_1.EntropyClient(connection, groupConfig.entropyProgramId);
    const entropyGroup = yield client.getEntropyGroup(groupConfig.publicKey);
    const entropyAccounts = yield client.getAllEntropyAccounts(entropyGroup, [], false);
    entropyAccounts.sort((a, b) => b[args.deposits_or_borrows][marketIndex]
        .abs()
        .cmp(a[args.deposits_or_borrows][marketIndex].abs()));
    const entropyCache = yield entropyGroup.loadCache(connection);
    for (let i = 0; i < 10; i++) {
        console.log(`${i}: ${entropyAccounts[i].toPrettyString(groupConfig, entropyGroup, entropyCache)}\n`);
    }
    process.exit(0);
})).argv;
(0, yargs_1.default)((0, helpers_1.hideBin)(process.argv)).command('show-perp-market <group> <symbol>', 'Print relevant details about a perp market', (y) => {
    return y
        .positional(...groupDesc)
        .positional('symbol', {
        describe: 'The ticker symbol of the perp market',
        type: 'string',
    })
        .option(...configDesc);
}, (args) => __awaiter(void 0, void 0, void 0, function* () {
    console.log('show-perp-market', args);
    const config = readConfig(args.config);
    const groupConfig = config.getGroupWithName(args.group);
    const perpMarketConfig = (0, utils_1.throwUndefined)((0, config_1.getPerpMarketByBaseSymbol)(groupConfig, args.symbol));
    const connection = openConnection(config, groupConfig.cluster);
    const client = new client_1.EntropyClient(connection, groupConfig.entropyProgramId);
    const entropyGroup = yield client.getEntropyGroup(groupConfig.publicKey);
    const perpMarket = yield client.getPerpMarket(perpMarketConfig.publicKey, perpMarketConfig.baseDecimals, perpMarketConfig.quoteDecimals);
    console.log(perpMarket.toPrettyString(entropyGroup, perpMarketConfig));
    process.exit(0);
})).argv;
(0, yargs_1.default)((0, helpers_1.hideBin)(process.argv)).command('verify-token-gov <token_account> <owner>', 'Verify the owner of token_account is a governance PDA', (y) => {
    return y
        .positional('token_account', {
        describe: 'the public key of the EntropyAccount',
        type: 'string',
    })
        .positional('owner', {
        describe: 'The owner of the token_account',
        type: 'string',
    })
        .option('program_id', {
        default: 'GqTPL6qRf5aUuqscLh8Rg2HTxPUXfhhAXDptTLhp1t2J',
        describe: 'Entropy DAO program id',
        type: 'string',
    })
        .option('realm', {
        default: 'DPiH3H3c7t47BMxqTxLsuPQpEC6Kne8GA9VXbxpnZxFE',
        describe: 'Realm of this governance',
        type: 'string',
    });
}, (args) => __awaiter(void 0, void 0, void 0, function* () {
    const programId = new web3_js_1.PublicKey(args.program_id);
    const realm = new web3_js_1.PublicKey(args.realm);
    const tokenAccount = new web3_js_1.PublicKey(args.token_account);
    const owner = new web3_js_1.PublicKey(args.owner);
    const [address] = yield web3_js_1.PublicKey.findProgramAddress([
        Buffer.from('token-governance', 'utf-8'),
        realm.toBuffer(),
        tokenAccount.toBuffer(),
    ], programId);
    if (address.equals(owner)) {
        console.log(`Success. The token_account: ${tokenAccount.toBase58()} is owned by a governance PDA`);
    }
    else {
        console.log(`Failure`);
    }
    process.exit(0);
})).argv;
(0, yargs_1.default)((0, helpers_1.hideBin)(process.argv)).command('change-perp-market-params <group> <symbol>', 'change params for a perp market', (y) => {
    return y
        .positional(...groupDesc)
        .positional(...symbolDesc)
        .option('maint_leverage', {
        type: 'number',
    })
        .option('init_leverage', {
        type: 'number',
    })
        .option('liquidation_fee', {
        type: 'number',
    })
        .option('maker_fee', {
        type: 'number',
    })
        .option('taker_fee', {
        type: 'number',
    })
        .option('rate', {
        type: 'number',
    })
        .option('max_depth_bps', {
        type: 'number',
    })
        .option('target_period_length', {
        type: 'number',
    })
        .option('mngo_per_period', {
        type: 'number',
    })
        .option('exp', {
        type: 'number',
    })
        .option('version', {
        type: 'number',
    })
        .option('lmSizeShift', {
        type: 'number',
    })
        .option(...clusterDesc)
        .option(...configDesc)
        .option(...keypairDesc);
}, (args) => __awaiter(void 0, void 0, void 0, function* () {
    console.log('change-perp-market-params', args);
    const account = readKeypair(args.keypair);
    const config = readConfig(args.config);
    const cluster = args.cluster;
    const connection = openConnection(config, cluster);
    const groupConfig = config.getGroup(cluster, args.group);
    const symbol = args.symbol;
    const client = new client_1.EntropyClient(connection, groupConfig.entropyProgramId);
    const entropyGroup = yield client.getEntropyGroup(groupConfig.publicKey);
    const perpMarketConfig = (0, utils_1.throwUndefined)((0, config_1.getPerpMarketByBaseSymbol)(groupConfig, symbol));
    const perpMarket = yield client.getPerpMarket(perpMarketConfig.publicKey, perpMarketConfig.baseDecimals, perpMarketConfig.quoteDecimals);
    // console.log(perpMarket.liquidityMiningInfo.rate.toString());
    // console.log(perpMarket.liquidityMiningInfo.mngoPerPeriod.toString());
    // console.log(perpMarket.liquidityMiningInfo.mngoLeft.toString());
    // console.log(perpMarket.liquidityMiningInfo.periodStart.toString());
    // console.log(perpMarket.liquidityMiningInfo.targetPeriodLength.toString());
    let mngoPerPeriod = getNumberOrUndef(args, 'mngo_per_period');
    if (mngoPerPeriod !== undefined) {
        const token = (0, config_1.getTokenBySymbol)(groupConfig, 'MNGO');
        mngoPerPeriod = (0, utils_1.uiToNative)(mngoPerPeriod, token.decimals).toNumber();
    }
    const exp = getNumberOrUndef(args, 'exp');
    if (exp !== undefined && !Number.isInteger(exp)) {
        throw new Error('exp must be an integer');
    }
    const version = getNumberOrUndef(args, 'version');
    if (exp !== undefined && !Number.isInteger(exp)) {
        throw new Error('version must be an integer');
    }
    const lmSizeShift = getNumberOrUndef(args, 'lmSizeShift');
    if (exp !== undefined && !Number.isInteger(exp)) {
        throw new Error('lmSizeShift must be an integer');
    }
    console.log(args);
    yield client.changePerpMarketParams2(entropyGroup, perpMarket, account, getNumberOrUndef(args, 'maint_leverage'), getNumberOrUndef(args, 'init_leverage'), getNumberOrUndef(args, 'liquidation_fee'), getNumberOrUndef(args, 'maker_fee'), getNumberOrUndef(args, 'taker_fee'), getNumberOrUndef(args, 'rate'), getNumberOrUndef(args, 'max_depth_bps'), getNumberOrUndef(args, 'target_period_length'), mngoPerPeriod, exp, version, lmSizeShift);
    // await sleep(2000);
    // perpMarket = await client.getPerpMarket(
    //   perpMarketConfig.publicKey,
    //   perpMarketConfig.baseDecimals,
    //   perpMarketConfig.quoteDecimals,
    // );
    // console.log(perpMarket.liquidityMiningInfo.rate.toString());
    // console.log(perpMarket.liquidityMiningInfo.mngoPerPeriod.toString());
    // console.log(perpMarket.liquidityMiningInfo.mngoLeft.toString());
    // console.log(perpMarket.liquidityMiningInfo.periodStart.toString());
    // console.log(perpMarket.liquidityMiningInfo.targetPeriodLength.toString());
    process.exit(0);
})).argv;
function getNumberOrUndef(args, k) {
    return args[k] === undefined ? undefined : args[k];
}
(0, yargs_1.default)((0, helpers_1.hideBin)(process.argv)).command('set-admin <group> <admin_pk>', 'transfer admin permissions over group to another account', (y) => {
    return y
        .positional(...groupDesc)
        .positional('admin_pk', {
        describe: 'the public key of the new group admin',
        type: 'string',
    })
        .option(...clusterDesc)
        .option(...configDesc)
        .option(...keypairDesc);
}, (args) => __awaiter(void 0, void 0, void 0, function* () {
    console.log('set-admin', args);
    const account = readKeypair(args.keypair);
    const config = readConfig(args.config);
    const cluster = args.cluster;
    const connection = openConnection(config, cluster);
    const groupConfig = config.getGroup(cluster, args.group);
    const client = new client_1.EntropyClient(connection, groupConfig.entropyProgramId);
    const entropyGroup = yield client.getEntropyGroup(groupConfig.publicKey);
    yield client.setGroupAdmin(entropyGroup, new web3_js_1.PublicKey(args.admin_pk), account);
    process.exit(0);
})).argv;
//# sourceMappingURL=cli.js.map